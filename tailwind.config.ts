import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
  theme: {},
  mode: "jit",
  important: "#notify-module",
  plugins: [],
};
export default config;
